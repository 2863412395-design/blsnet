[project]
name = "blsnet"
version = "0.1.0"
description = "Boundary-Guided Lite-Token Segmentation Network"
requires-python = ">=3.9"
dependencies = [
  "torch>=2.0",
  "torchvision>=0.15",
  "numpy>=1.23",
]

[tool.setuptools.packages.find]
where = ["src"]
